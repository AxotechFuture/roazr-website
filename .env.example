# Airtable — where demo-form leads are stored.
# Create a Personal Access Token at https://airtable.com/create/tokens
# with data.records:write scope on your leads base.
AIRTABLE_API_KEY=
AIRTABLE_BASE_ID=
# Optional; defaults to "Leads"
AIRTABLE_TABLE_NAME=Leads
