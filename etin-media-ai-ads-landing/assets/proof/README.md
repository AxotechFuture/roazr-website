# Before/After ad-account proof images

Drop two image files here and the landing page swaps them in automatically
(no code change needed):

- `before.png`  — the June ad-account screenshot (weak results)
- `after.png`   — the July ad-account screenshot (548/403/363 sales)

Accepted: .png or .jpg (rename to before.png / after.png). Any size — the page
scales them. If a file is missing, the page falls back to the clean HTML
recreation of the same numbers.
